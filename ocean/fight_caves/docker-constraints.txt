# Core versions verified with Ubuntu 24.04, CUDA 13.0 and the Fight Caves config.
# Install torch from https://download.pytorch.org/whl/cu130 before PufferLib.
torch==2.9.1+cu130
numpy==2.5.3
pybind11==3.1.0
pytest==9.1.1
# This branch calls wandb.util.generate_id(), removed by newer W&B releases.
wandb==0.28.1
